#!/bin/bash
# Warm listen daemon launcher
LISTEN_DIR="$HOME/.local/share/listen"
ROUTER_DIR="$HOME/.local/share/voice-router"
source "$LISTEN_DIR/venv/bin/activate"
PYTHONPATH="$LISTEN_DIR" exec python "$ROUTER_DIR/listen_daemon.py" "$@"

#!/bin/bash
# Named Claude Code session launcher — sets iTerm2 tab title and user variable
NAME="${1:?Usage: cc <name> [claude-args...]}"
shift
printf '\033]1;%s\007' "$NAME"
printf '\033]1337;SetUserVar=cc_name=%s\007' "$(echo -n "$NAME" | base64)"
exec claude --name "$NAME" "$@"
